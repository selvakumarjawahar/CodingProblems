//
// Created by selva on 9/25/21.
//

#pragma once
#include <vector>
#include <functional>

template<typename T, typename Predicate = std::less<>>
std::vector<T> MergeSort(const std::vector<T>& to_sort, Predicate comp = Predicate{})
{
  
  return to_sort;

}
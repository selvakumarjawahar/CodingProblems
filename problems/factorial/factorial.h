/*

    The problem statement is to generate a factorial sequence, when provided with number of elements required for the factorial
*/


#pragma once

#include <vector>
#include <cstddef>

size_t GenerateFactorial(int num);
std::vector<int> GenerateFactorialSequence(int sequence_size);


